import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Inscription | RÉSEAUTEURS',
  description: 'Créez votre compte réseauteur gratuit sur RÉSEAUTEURS — la plateforme nationale du networking.',
}

export default function InscriptionLayout({ children }: { children: React.ReactNode }) {
  return children
}
